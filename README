1) "mvn clean package exec:java -DnodeType=Server"
2) "mvn clean package exec:java -DnodeType=Proxy"
3) "mvn clean package exec:java -DnodeType=Client"
4) if you don't get the issue with 3500 clients then add some in the "StartClient" class (the AMOUNT_OF_CLIENTS variable).